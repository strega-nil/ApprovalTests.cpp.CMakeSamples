# develop_approvaltests

include: develop_approvaltests

The top-level CMakeLists.txt file is:

include: inc_develop_approvaltests_cmakelists

The CMakeLists.txt to pull in dependencies is:

include: inc_develop_approvaltests_dependencies_cmakelists

And the CMakeLists.txt that builds the tests is:

include: inc_develop_approvaltests_tests_cmakelists
